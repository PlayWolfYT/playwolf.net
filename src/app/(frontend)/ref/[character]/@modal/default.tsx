/**
 * Nothing is open. Rendered for every `/ref/<character>/…` URL that was not
 * reached by clicking a thumbnail, including full page loads of an artwork.
 */
export default function ModalDefault() {
  return null;
}
