import { playfang } from "./playfang";
import { playwuff } from "./playwuff";
import type { Character } from "@/data/types";

/** All characters surfaced under `/ref`. Add new characters here. */
export const characters: Character[] = [playwuff, playfang];
